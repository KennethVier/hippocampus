spring:
  config:
    activate:
      on-profile: test
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate

hippocampus:
  materials:
    processing:
      recovery:
        enabled: false
  security:
    cors:
      allowed-origins: http://localhost:5173

server:
  port: 0

logging:
  structured:
    ecs:
      service:
        environment: test

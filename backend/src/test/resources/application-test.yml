spring:
  config:
    activate:
      on-profile: test
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 0

logging:
  structured:
    ecs:
      service:
        environment: test

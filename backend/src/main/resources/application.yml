spring:
  application:
    name: hippocampus-backend
  autoconfigure:
    exclude:
      - org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration
  flyway:
    enabled: false
    locations: classpath:db/migration
    clean-disabled: true
  session:
    timeout: ${HIPPOCAMPUS_SESSION_IDLE_TIMEOUT:PT30M}
    jdbc:
      initialize-schema: never

hippocampus:
  materials:
    processing:
      pdf:
        page-batch-size: ${HIPPOCAMPUS_PDF_PAGE_BATCH_SIZE:20}
        max-pages: ${HIPPOCAMPUS_MAX_PDF_PAGES:2000}
        max-native-text-chars-per-page: ${HIPPOCAMPUS_PDF_MAX_NATIVE_TEXT_CHARS_PER_PAGE:1000000}
  security:
    login:
      max-attempts: 60
      window: PT1M
      maximum-tracked-addresses: 1024

server:
  address: 127.0.0.1
  servlet:
    session:
      cookie:
        http-only: true
        same-site: lax
        path: /api

management:
  endpoints:
    access:
      default: none
    web:
      discovery:
        enabled: false
      exposure:
        include: health
    jmx:
      exposure:
        exclude: "*"
  endpoint:
    health:
      access: unrestricted
      show-details: never
      show-components: never
      probes:
        enabled: true
      group:
        liveness:
          include: livenessState
        readiness:
          include: readinessState

logging:
  level:
    root: INFO
    org.springframework.web: INFO
  structured:
    format:
      console: ecs
    ecs:
      service:
        environment: base

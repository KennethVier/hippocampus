package com.hippocampus.learning.domain;

public enum LearningActionType {
    START,
    RESUME,
    UNDERSTAND,
    RETRIEVE,
    CONNECT,
    APPLY,
    RETRY,
    HINT,
    PREREQUISITE_SUPPORT,
    REDUCE_DIFFICULTY,
    FEEDBACK,
    REFLECT,
    REUSE_VALIDATED_CONTENT,
    SOURCE_ONLY,
    COMMUNICATE_LIMITATION,
    RETRY_DEPENDENCY,
    PAUSE,
    COMPLETE,
    STOP
}
